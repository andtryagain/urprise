import React from 'react';

const ShowList = (list) => {
    return (
        <>
            <Content>
                <h1>{list.title}</h1>
            </Content>
        </>
    );
}

export default ShowList;